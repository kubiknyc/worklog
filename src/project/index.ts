/** Active-project seam — screens import the provider/hook from here. */
export {
  ActiveProjectProvider,
  useActiveProject,
  type ActiveProjectContextValue,
} from './ActiveProjectProvider';
export { resolveActiveProject } from './resolveActiveProject';
