# JobSight Supabase project this site's pages talk to (the /welcome
# set-password page, added in a later task). Both values are safe to expose
# to the browser — the anon key is the publishable client key, not a secret.
NEXT_PUBLIC_SUPABASE_URL=https://nxlznnrocrffnbzjaaae.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Copy to .env.local for local dev (gitignored). Production values are set
# in the Vercel project's environment variables.
