# Auth Parity — Backend Audit + Deploy Plan (3 of 3)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox syntax.

**Goal:** Make registration work end to end via **fully namespaced, purely additive** backend artifacts: `worklog-register-company` + `worklog-send-email` (WorkLog-branded fork — shared `send-email` never touched), plus WorkLog secrets and GoTrue allowlist appends, validated with real round-trips.

**Architecture:** Audit-first (user decision, spec Resolved decisions). Server code: `../jobsight-backend`, branch `worklog-register` off its default branch. Hosted project `nxlznnrocrffnbzjaaae` is shared with another app; **zero replace-class changes**: no shared function redeployed, no existing secret value modified, no existing template touched. Shared-state classes: (a) replace-forbidden — existing secrets' values, deployed functions, template keys, GoTrue Site URL; (b) append-permitted — GoTrue `uri_allow_list`: append adds exactly three WorkLog entries; **rollback removes exactly those three from the then-current value** (never restores a snapshot; abort + escalate if not found verbatim). Spec: `docs/superpowers/specs/2026-08-07-punchlog-auth-parity-design.md` (§7, A1–A3, A6, Resolved decisions). Santa escalation resolved by user 2026-08-11: fork, don't patch. **Review status:** three santa loops run; loop-3 round-1 (worklog-reviewer/Opus + codex, both FAIL) found a false-green gate assertion and a missing shared-auth precondition — both fixed below. **The candidate GoTrue allowlist entries, verbatim (referenced throughout as "the entries"):** `https://worklog-site.vercel.app/welcome`, `worklog://set-password`, `worklog://confirm`. **Each must be justified by a live minter before it is appended — an unjustified entry is unexplained widening of shared state and is dropped, not appended.** Established: `https://worklog-site.vercel.app/welcome` is minted by both fork redirect sites (A1); `worklog://set-password` is minted by the app's own password reset (`src/auth/AuthProvider.tsx:306`, `resetPasswordForEmail({redirectTo})`) — independent of this plan's functions but a real product path. **`worklog://confirm` has NO minter in the app repo** (it is only *parsed*, by `src/auth/confirmLanding.ts`, and nothing sets it as a `redirectTo`), so **T1 must find a minter or the entry is dropped from the append set.** **Cross-tenant note, disclosed in T4's approval:** `uri_allow_list` is project-global, so every appended entry also becomes a legal `redirect_to` for the OTHER tenant's GoTrue links — a PunchLog-issued confirm/recovery link could carry `redirect_to=worklog://set-password` and deliver a PunchLog token into the WorkLog app's deep-link handler. This is the strongest argument for appending the minimum justified set.

**Tech Stack & required credentials:** Supabase MCP (read-only audit: `list_edge_functions`, `list_tables`, `execute_sql`; branch lifecycle: `get_cost`/`confirm_cost`/`create_branch`/`delete_branch`; migrations: `list_migrations` + `apply_migration`); **Supabase Management API with a personal access token `$SUPABASE_ACCESS_TOKEN` — collected from the user at the T1 gate** (auth config: `GET/PATCH /v1/projects/{ref}/config/auth`; secrets: `POST /v1/projects/{ref}/secrets` — the MCP has no secrets tool; deploys: `POST /v1/projects/{ref}/functions/deploy?slug={slug}` with multipart source files + a `metadata` JSON part carrying `{name, entrypoint_path, verify_jwt}` — **`name` in the metadata, `slug` only as the query param**; an earlier draft said `slug` in both places — the MCP deploy tool cannot set `verify_jwt`; after every deploy, GET the function record and assert the returned `verify_jwt` matches intent); Vercel CLI (site env check); **Deno (runs the fork's `deno test` A1 proofs in T2)**; `../jobsight-backend`; WorkLog app repo (one seam commit). Production `$SUPABASE_URL` and `$ANON_KEY` for T5 curls come from MCP `get_project_url` + `get_publishable_keys` (never inlined into the plan or commits). **Precondition checked before T5 runs any curl:** `worklog-register-company` is `verify_jwt=true`, so the key must be one GoTrue accepts as a JWT — if `get_publishable_keys` returns a non-JWT `sb_publishable_…` key, every curl 401s for a reason unrelated to the fork. Verify with a single throwaway bad-field curl expecting a **400**, not a 401, before spending the schedule; if it 401s, obtain a legacy anon JWT (`GET /v1/projects/{ref}/api-keys?reveal=true`) and record the substitution.

## Global Constraints

- **Namespaced artifacts only:** new slugs `worklog-register-company` (`verify_jwt=true`; every curl sends `Authorization: Bearer $ANON_KEY` + `apikey: $ANON_KEY`) and `worklog-send-email` (`verify_jwt=false`; `x-email-secret` auth). Neither generic slug is ever claimed. Curl template (keys from env, never inlined):
  `curl -s -X POST "$SUPABASE_URL/functions/v1/worklog-register-company" -H "Authorization: Bearer $ANON_KEY" -H "apikey: $ANON_KEY" -H "Content-Type: application/json" -d '{"companyName":"...","fullName":"...","email":"...","client":"app"}' -o rN.json` — byte-identical-200 checks are `diff r1.json r2.json`.
- **WorkLog secrets (all new; T1 confirms none pre-exist, else escalate):** `WORKLOG_WEBSITE_URL=https://worklog-site.vercel.app`; `WORKLOG_EMAIL_SHARED_SECRET` (fresh 44-char, shared by exactly the two worklog functions, ≥32-char check BOTH sides); `WORKLOG_RESEND_FROM` (T1 gate decision); `WORKLOG_ALLOWED_ORIGINS` (fork of the CORS list at index.ts:144-151 — `DEV_ORIGINS` :144, `const ALLOWED_ORIGINS = new Set([...])` :145-151 — note the source seeds `DEV_ORIGINS` (localhost:3000/8081/19006) regardless of the secret, so "unset" still echoes localhost dev origins; the fork keeps that behavior, which is harmless). **Shared-by-name reads (exhaustive carve-out list): `RESEND_API_KEY` only.** Values set via `POST /v1/projects/{ref}/secrets`.
- **A1 (mechanically fail-closed):** both redirect sites in the fork (of index.ts:133-135, `APP_CONFIRM_FALLBACK_URL` deleted; of the ternary at :267-272) read `WORKLOG_WEBSITE_URL`; startup validation 503s before any quota RPC / GoTrue call / link generation unless the parsed URL has `protocol==='https:'`, host `worklog-site.vercel.app`, no port, `pathname==='/'`, empty search AND hash; links are built from the parsed canonical origin.
- **A3:** the fork keeps standard template keys and posts ONLY to `worklog-send-email` — the edit is the single `sendEmail` helper URL line (fork of :261); the four call sites (:313, :341, **:395 signup-race**, :424) are enumerated for reviewer verification that none bypasses the helper.
- **Branding rule:** shell + templates rebranded (WorkLog wordmark/footer, blueprint-accent button — not #C8102E), `from: WORKLOG_RESEND_FROM`. Site-owned links point only at `/welcome`, `/terms`, `/privacy`; the confirm CTA href is GoTrue's `action_link` (`*.supabase.co/auth/v1/verify?...redirect_to=<welcome>`) — expected and allowed. No `/forgot-password`, `/download`, `/register` links in any form, including `${VAR}/path` interpolations ("lost your password" copy points at the app's Forgot password button).
- **Production gate:** T3/T4/T5 each require explicit user approval naming the specific actions. T3's approval covers branch creation AND deletion AND possible real email via the shared Resend key.
- **Management API call shapes (no step may invent these).** Base `https://api.supabase.com`, header `Authorization: Bearer $SUPABASE_ACCESS_TOKEN`. Auth config: `GET /v1/projects/{ref}/config/auth` → full JSON body; `PATCH` same path, `Content-Type: application/json`, body carrying **only** the keys being changed (`{"uri_allow_list":"<csv>"}`). Secrets: `POST /v1/projects/{ref}/secrets`, body `[{"name":"...","value":"..."}]`; list `GET /v1/projects/{ref}/secrets` (**returns names plus hashed digests, not reusable plaintext** — use it for vacancy/name checks only, which is a further reason T3 takes a user-supplied Resend key rather than copying production's); delete `DELETE /v1/projects/{ref}/secrets` body `["NAME",...]`. Functions: deploy `POST /v1/projects/{ref}/functions/deploy?slug={slug}` multipart with a `metadata` JSON part `{"name","entrypoint_path","verify_jwt"}` plus each source file as a `file` part; read back `GET /v1/projects/{ref}/functions/{slug}`; delete `DELETE /v1/projects/{ref}/functions/{slug}`. Project API keys (branch anon + service_role): `GET /v1/projects/{ref}/api-keys?reveal=true`. Every write is followed by its paired read-back assertion; a read-back that disagrees is an abort, not a warning.
- **Shared-config mutation discipline (every `config/auth` PATCH, branch or production).** GoTrue reloads on PATCH — **an auth-side blip the other tenant experiences; disclosed in T4's approval alongside the secrets restart** (and the second restart caused by rollback's secret deletion). The allowlist edit is a read-modify-write on shared state, so: GET the **full** auth config immediately before the PATCH; check which of the justified entries are already present. **A pre-existing entry is a likely, benign state — `worklog://set-password` may already serve the app's invite/recovery landing — so it does not dead-end the plan:** define `ADDED` as exactly the **missing subset**, record which entries were pre-existing, and scope rollback to remove **only `ADDED`** (a pre-existing entry is never removed). If `ADDED` is empty, the PATCH is skipped entirely and that is recorded as success, not as a no-op failure; PATCH; GET back and assert (a) every member of `ADDED` present byte-for-byte, (b) every pre-existing allowlist entry still present, (c) **every other field of the auth config byte-identical to the pre-PATCH snapshot** — "Site URL untouched" is an *assertion*, not an intention. If the post-PATCH body differs anywhere else, or the pre-PATCH allowlist differs from the value T1 recorded, treat it as a concurrent tenant change: do not retry blind — re-read, re-diff, escalate. **Stated limit, not papered over:** the Management API offers no conditional/if-match write, so this read-modify-write cannot be made atomic, and `ADDED` ownership is only as good as "no one else edited between our GET and PATCH". A concurrent change detected at any point is therefore a **STOP**, not a retry; and if another tenant independently adds an entry also in `ADDED` after our PATCH, rollback would remove a value they now also rely on — so **rollback re-reads and escalates rather than removing when the live value has changed since our own post-PATCH read**.
- **User-decision gate ending Task 1 (unconditional; answers recorded as a spec amendment BEFORE Task 2):** (1) **A2** — may WorkLog registrants enter the shared `auth.users` pool? **A "no" answer kills Tasks 2–5 as written** (every artifact here registers into that pool): STOP, record the answer as the spec amendment, and escalate to a separate-project / separate-GoTrue design — do not attempt a workaround inside this plan. (2) **A6** — approve deferring both halves; **the only two permitted answers are "defer" (this plan proceeds with `WORKLOG_ALLOWED_ORIGINS` unset, three secrets not four) and "STOP" (a web deployment exists or is wanted ⇒ this plan halts and a follow-up covering the browser surface is planned separately)** — naming a deployment mid-plan is not an executable third option, because Tasks 2–5 contain no CORS validation path. (No WorkLog web deployment exists; T1 verifies the website never calls the function and that `/welcome` hits GoTrue directly, which is what puts `ALLOWED_ORIGINS` off the critical path and makes "defer" safe.) (3) **Sender** — `WORKLOG_RESEND_FROM`: a `worklog@{Resend-verified domain — user reads it off their Resend dashboard}` address (same-domain needs no new verification), **or `onboarding@resend.dev` — WARNING: it delivers only to the Resend account owner's own address, so choosing it DESCOPES T5's Gmail-alias assertions to owner-address sends and is testing-only**. Also collected here: `$SUPABASE_ACCESS_TOKEN` for the management API.
- **Quotas (fork keeps both):** `EMAIL_DAILY_LIMIT` 3/day/address; `IP_DAILY_LIMIT` 20/day/hashed-IP (index.ts:100). Field-validation 400s (the `client` check at index.ts:204-206 plus the name/email checks at :209-215) all precede the quota RPC at :232 and cost nothing. **Curl schedule** (fits both quotas): T3 branch — wl-a fresh(app), wl-a re-register after admin-confirm (=confirmed-dup), wl-b fresh(web), wl-c fresh + wl-c re-register (=unconfirmed-dup/magiclink), wl-d ×4 (429 check), bad-field ×1 → 10 curls, aliases distinct. T5 production — wl1 fresh(app), wl2 fresh(web), wl1 re-register (confirmed-dup), wl3 fresh + wl3 re-register (magiclink), bad-field ×1 → 6 curls. Never reset quota rows in production.

---

### Task 1: Read-only preflight audit (no writes anywhere)

Produce `docs/superpowers/specs/2026-08-11-backend-audit.md` with SQL/MCP evidence per bullet:

- [ ] **Function inventory:** `list_edge_functions` — all slugs + versions; `worklog-register-company` / `worklog-send-email` vacant (else escalate); note `register-company` presence and shared `send-email` version (reference only).
- [ ] **Schema:** `profiles` (incl. `email`, `full_name`), `companies` (`created_by`), company-membership table, triggers **`handle_new_user`** (index.ts:63) and **`bootstrap_company_creator`** (index.ts:67, :412-416 — absent trigger ⇒ company row without membership/admin enrolment). **Escalation branch:** if `handle_new_user` does NOT populate `profiles.email`, the duplicate lookup (index.ts:353-356) never matches and every duplicate path falls into the signup-race branch (:382-402) — T3/T5's duplicate assertions become unreachable; STOP and escalate with options (add a WorkLog migration vs accept degraded duplicate handling) before proceeding. **If "add a migration" is chosen, it lands in `../jobsight-backend/supabase/migrations` on `worklog-register`** (never in this repo), and the snapshot commit is **deferred until that sibling branch merges to the ref CI regenerates from**. Ordering, because getting it wrong breaks the repo's own gates: `npm run verify` (`package.json:15`) is typecheck + format:check + lint + test and does **not** read the sibling at all — only `check:parity` (`package.json:17`) regenerates and diffs, and `src/db/schemaParity.test.ts` reads the *committed* `serverColumns.generated.json`. So committing a regenerated snapshot early fails CI's `schema-parity` job (the sibling's shipped ref lacks the migration, so the diff is dirty) and can fail `schemaParity.test.ts` itself, whose new server columns must be mirrored in `DOMAIN_COLUMNS` or declared in that test's `LOCAL_ONLY`/`SERVER_ONLY` maps **in the same commit** — never by loosening the assertion. Sequence: sibling migration merged → `npm run gen:server-columns` → snapshot + any `SERVER_ONLY` declaration committed together.
- [ ] **RPC:** `consume_registration_quota(p_key, p_limit)` exists.
- [ ] **Allowlist minter search (owns the Architecture's justification rule; without this step the rule has no executing owner):** search the app repo AND the website source for anything that sets each candidate entry as a `redirectTo`/`emailRedirectTo`. Established so far: `https://worklog-site.vercel.app/welcome` ← both fork redirect sites (A1); `worklog://set-password` ← `src/auth/AuthProvider.tsx:306` (`resetPasswordForEmail`). **`worklog://confirm` — no minter found in the app repo (it is only parsed, by `confirmLanding.ts`/`authLink.ts`).** Outcome is binary and recorded: a minter is found ⇒ the entry joins the justified set; none ⇒ **the entry is dropped** and the justified set is two. This fixes the justified set that `ADDED` is computed from in T3 and T4.
- [ ] **Key format + existing-function flags:** record the *format* of the anon key the app ships (`EXPO_PUBLIC_*`) and what `get_publishable_keys` returns, plus the existing `register-company`'s `verify_jwt`. **If the project issues non-JWT `sb_publishable_…` keys, a `verify_jwt=true` fork 401s the app's own `functions.invoke` — and T5's curls would not catch it, since they can be re-run with a legacy JWT.** Escalate before T2 fixes `verify_jwt=true`: either confirm the shipped key is a JWT, or set the fork's flag to match what the existing `register-company` uses.
- [ ] **Other-tenant lifecycle:** ALL triggers on `auth.users`; would a WorkLog registrant enter the other app's approval queue/tables/UI?
- [ ] **Secrets (names only, never values):** `RESEND_API_KEY` exists; `WEBSITE_URL`/`EMAIL_SHARED_SECRET`/`RESEND_FROM`/`ALLOWED_ORIGINS` noted for reference; no `WORKLOG_*` secret pre-exists (else escalate).
- [ ] **Resend readiness:** from the user at the gate: which domain is Verified in their Resend dashboard; confirm sending quota headroom for ~16 mails; record that `onboarding@resend.dev` delivers only to the account owner (drives the Sender decision's descope warning).
- [ ] **Branching availability + build inputs:** hosted project supports preview branches (plan/billing) BEFORE T3 asks the user to pay; if enabling branching is itself a project-level state change, surface it in T3's approval. **Also establish what a branch would actually be built from:** is the project git-integrated, to which repo/branch, and does a preview branch track `../jobsight-backend`'s pushed refs? A Supabase branch is built from migrations in a **pushed** git ref — if there is no integration, T3 cannot be built as written and must be re-scoped at the T3 approval (options: enable integration, or descope T3 to a local `supabase start` rehearsal with no paid branch). Record which.
- [ ] **GoTrue config (save the FULL body, it is T4's comparison baseline):** `GET /v1/projects/{ref}/config/auth` — record Site URL and `uri_allow_list` verbatim, record **which of the three WorkLog entries (if any) already exist** — this fixes the append set and the rollback ownership set per the shared-config discipline above — and record **`mailer_autoconfirm` / "Enable email confirmations"**. **Escalation branch:** if autoconfirm is ON (confirmations disabled), `admin.generateLink({type:'signup'})` returns an already-confirmed user — (a) any anonymous caller could create a *confirmed* account for an address they do not control, the exact attack register-company:70-74 exists to prevent, and (b) the unconfirmed-duplicate/magiclink branch (index.ts:296, gated on `!email_confirmed_at`) becomes unreachable, so T3's and T5's magiclink assertions test something that cannot happen. STOP and escalate — turning confirmations on is a **replace-class change to shared auth** and is not this plan's to make.
- [ ] **Website prerequisite:** curl `/welcome` `/terms` `/privacy` → 200 AND `npx vercel env ls production` shows both `NEXT_PUBLIC_SUPABASE_*` vars present; confirm from website source (already in-repo) that `/welcome` calls GoTrue directly and never the edge function (grounds the A6 deferral).
- [ ] **Verdict:** GO / NO-GO with each collision named. **Then the three-question user gate + `$SUPABASE_ACCESS_TOKEN` collection; write answers into the spec as an amendment. STOP until done.**

### Task 2: Fork the two functions (code only, sibling repo)

On `../jobsight-backend` branch `worklog-register`. Task artifact: `docs/worklog-fork-report.md` in that repo (gate output pasted there).

- [ ] `supabase/functions/worklog-register-company/` = copy of `register-company/` with: both redirect sites on `WORKLOG_WEBSITE_URL`; `APP_CONFIRM_FALLBACK_URL` deleted; startup canonical-origin validation (503 pre-side-effect per A1); `EMAIL_SHARED_SECRET` → `WORKLOG_EMAIL_SHARED_SECRET` (≥32-char check); `ALLOWED_ORIGINS` → `WORKLOG_ALLOWED_ORIGINS` — **the local `const ALLOWED_ORIGINS` (:145) is renamed too, not just the `Deno.env.get` key**, since the gate's `[A-Z_]*ALLOWED_ORIGINS` extraction counts the identifier; `sendEmail` helper URL → `/functions/v1/worklog-send-email`; header comments rewritten.
- [ ] `supabase/functions/worklog-send-email/` = copy of `send-email/` with: shell + `confirm_signup`/`account_exists` rebranded (Branding rule); **all `${WEBSITE_URL}/forgot-password|/download|/register` interpolated links removed** (send-email/index.ts:129, 189, 197, 225 — copy says "use the app's Forgot password button"); **comment lines mentioning those routes also rewritten in BOTH forks** (the gate matches comments too: send-email:47/122/125/212, register-company:5/:36 in the source); **plus the header comment send-email:29 (`POST /functions/v1/send-email`) → `/functions/v1/worklog-send-email`** — the shared-function guard is occurrence-based and counts comments, so leaving :29 fails the fork's own gate; `RESEND_FROM` → `WORKLOG_RESEND_FROM`; `EMAIL_SHARED_SECRET` → `WORKLOG_EMAIL_SHARED_SECRET` (≥32-char check kept); `WEBSITE_URL` → `WORKLOG_WEBSITE_URL`; unused templates deleted.
- [ ] **Gate script `scripts/worklog-a1-gate.sh`** (committed; `set -euo pipefail`; every check is a `test`/`[ ]` assertion that EXITS NON-ZERO on failure — counts alone gate nothing; output pasted in the fork report). **A non-zero exit is a hard blocker on entering T3 — no branch is created, nothing is deployed, until the gate and both `deno test` suites exit 0.** Pasting the output is evidence, not the gate. `A=supabase/functions/worklog-register-company; B=supabase/functions/worklog-send-email` (explicit operands on every grep). Each zero-expectation records its **pre-fork count against the unforked sources** in a comment, proving non-vacuity:
  **Assertions are one per statement — NEVER `[ a ] && [ b ]` (under `set -e` a failing left operand of `&&` does not exit; verified). Pre-fork counts below were measured against the unforked sources (loop-2 r3 review) and prove each zero-expectation is non-vacuous — the script's comments carry them.**
  - `[ "$(grep -rinE 'punchlist|punch.?log|punch list|#C8102E|APP_CONFIRM_FALLBACK_URL' $A $B | wc -l)" -eq 0 ]` (pre-fork: 30 lines)
  - `[ "$(grep -rnoE '/(forgot-password|download|register)\b' $A $B | wc -l)" -eq 0 ]` (occurrence-based via `-o`; catches `${VAR}/forgot-password` interpolations AND comments — hence T2's comment-rewrite instruction; pre-fork: 13 occurrences on 10 lines)
  - `[ "$(grep -rnoE '[A-Z_]*WEBSITE_URL' $A $B | grep -v WORKLOG_WEBSITE_URL | wc -l)" -eq 0 ]` (extraction, not line-filtering; pre-fork: 24; same shape for `EMAIL_SHARED_SECRET`, `RESEND_FROM`, `ALLOWED_ORIGINS`)
  - `[ "$(grep -rnE 'functions/v1/send-email\b' $A $B | wc -l)" -eq 0 ]` (separate statement — this is the shared-function guard and must never be short-circuited; **pre-fork: 2** — register-company:261 *and the header comment* send-email:29, which is why T2's comment-rewrite list names :29)
  - `[ "$(grep -rn 'functions/v1/worklog-send-email' $A/handler.ts | grep -vE '^[0-9]+:[[:space:]]*(//|\*)' | wc -l)" -eq 1 ]` (exactly one helper target in *code*, not merely ≥1; comment-stripped so the fork's rewritten header comment may name the new endpoint the way send-email:29 names the old one — T2 requires exactly that rewrite, and an un-stripped count would stall on a compliant fork)
  - `[ "$(grep -nE 'await sendEmail\(' $A/handler.ts | grep -vE '^[0-9]+:[[:space:]]*(//|\*)' | wc -l)" -eq 4 ]` (all four call sites still route through the helper — forks of :313, :341, :395, :424; comment-filtered, or a rewritten header comment inflates the count)
  - **A1 is proven BEHAVIOURALLY, not by grep.** Two retired approaches, both false-green, recorded so neither is reintroduced: (i) `grep -qE "503"` — the unforked source already 503s at register-company:255-257 (missing `EMAIL_SHARED_SECRET`), *and that 503 sits AFTER the quota RPCs at :232/:243*, so it proves the opposite of what A1 claims; (ii) a `grep -n "worklog-site.vercel.app" | head -1` line-anchor plus a ±25-line word-presence window — T2 orders the header comments rewritten, so a comment reading "validated at startup: protocol https, host worklog-site.vercel.app, pathname '/', empty search and hash, otherwise 503" satisfies every word AND drives the anchor to a tiny line number, making any dominance comparison trivially true with no validator in the file.
    **A third false-green, also retired:** exporting the validator, unit-testing it in isolation, then anchoring a line-number comparison on `assertCanonicalWorklogOrigin(`. With `head -1` that anchor matches the **definition**, which is module-scope and therefore always precedes every side effect — so a fork that defines, exports and passes unit tests on a validator it *never calls* goes green. Testing a pure function proves its predicate, never its invocation.
    **Required fork structure (so the property is testable at all) — three files, because `Deno.serve` at module scope (source :178) makes any module containing it unimportable from a test (no `--allow-net` ⇒ throws; with it ⇒ binds a real port):**
    - `$A/origin.ts` — `export function assertCanonicalWorklogOrigin(raw: string | undefined): URL`, pure, throws on any A1 violation.
    - `$A/handler.ts` — **all request logic, and the injection seam the tests need**: `export function createHandler(deps: {createAdminClient: () => SupabaseClient; fetch: typeof globalThis.fetch; env: (k: string) => string | undefined}): (req: Request) => Promise<Response>`, plus `export const handler = createHandler(defaultDeps)`. The unforked source builds its admin client inline at :218 (`createClient(SUPABASE_URL, SERVICE_ROLE_KEY)`) with no seam; the fork must lift that, `fetch`, and env reads into `deps` — otherwise "assert zero calls" has nothing to observe and the executing agent invents the seam the gate then measures.
    - `$A/index.ts` — imports `handler` and calls `Deno.serve(handler)`. Nothing else. **An earlier draft kept `Deno.serve` in the same file it told the test to import, contradicting its own stated reason for splitting `origin.ts`.**
    All static `CODE()` anchors below therefore target **`$A/handler.ts`**, not `index.ts`.
  - **A1 proof — three assertions, each a separate statement** (Deno is a required tool; it is listed in the Tech Stack):
    1. *Predicate* — `deno test --allow-env $A/origin.test.ts`: accepts exactly `https://worklog-site.vercel.app` and its trailing-slash form; **rejects** `http://…`, `https://evil.com`, `https://worklog-site.vercel.app.evil.com`, `https://worklog-site.vercel.app/welcome`, `https://worklog-site.vercel.app/?x=1`, `https://worklog-site.vercel.app/#f`, `""`, and unset. **Port:** `new URL("https://host:443").port` normalizes to `""`, so a parsed-port check *cannot* reject `:443` — the validator must reject it on the **raw authority** (the substring between `//` and the first `/` must equal the host exactly), and the test asserts `https://worklog-site.vercel.app:443` rejects. An earlier draft listed that case against a parsed-port check, where a correct validator would have failed it.
    2. *Invocation and ordering* — `deno test $A/handler.test.ts` (no permission flags needed: env is injected via `deps.env`, and `createHandler` is imported from `handler.ts`, which never serves). This is the only check that proves what A1 actually claims, and it needs **three cases, not one**:
       - **Full env fixture on every case.** `deps.env` returns a *valid* ≥32-char `WORKLOG_EMAIL_SHARED_SECRET`, plus `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` — everything the handler reads at or before the validator. **Otherwise the test is false-green**: the fork keeps the shared-secret guard (fork of :255-257), which also returns 503 with zero calls to all three probes, so a fork containing *no canonical-origin validator at all* would pass on the wrong 503. That is the same defect class as (i) and (ii), reached a fourth way.
       - **Negative:** hostile `WORKLOG_WEBSITE_URL` (each of the predicate-test rejects, table-driven) → assert **503** AND `deps` recorded **zero** calls to the quota RPC, `generateLink`, and `fetch`.
       - **Positive control (pins the 503 to the origin check):** canonical `WORKLOG_WEBSITE_URL` → assert the response is **not** 503 AND the probes **do** fire. Without this, a handler that 503s unconditionally passes the negative cases.
       - **Discrimination:** the origin-failure 503 carries a distinct log/marker from the shared-secret 503, and the negative cases assert that marker — so the two 503s can never be confused.
       A validator that exists but is never called fails here; one called *after* the quota RPC fails here; one whose 503 is really the secret guard fails here. Neither grep nor a pure-function test catches any of the three.
    3. *Static, secondary only* — anchored on the **call site**, never the definition, and run against `handler.ts`: `CODE()` = `grep -nE "$1" $A/handler.ts | grep -vE '^[0-9]+:[[:space:]]*(//|\*)' | head -1 | cut -d: -f1`. The comment filter is **anchored to the `grep -n` line-number prefix** — an unanchored `:[[:space:]]*(//|\*)` matches any line containing `https://` and silently discards real code. One statement each: `V=$(CODE "assertCanonicalWorklogOrigin\("); [ -n "$V" ]`; `Q=$(CODE "consume_registration_quota"); [ -n "$Q" ]`; `G=$(CODE "generateLink\("); [ -n "$G" ]`; `S=$(CODE "await sendEmail\("); [ -n "$S" ]` — **`await sendEmail(`, not `sendEmail(`**, so a fork that rewrites the helper as `function sendEmail(` cannot re-anchor `S` on its own definition and reintroduce the definition-vs-call-site false-green. Then `[ "$V" -lt "$Q" ]`, `[ "$V" -lt "$G" ]`, `[ "$V" -lt "$S" ]`. `Q`/`G`/`S` use `head -1` (FIRST occurrence) so a validator inserted between the IP RPC (:232) and the email RPC (:243) cannot pass.
  - `[ "$(grep -rn 'WORKLOG_WEBSITE_URL' $A | wc -l)" -ge 1 ]` (the old `-ge 3` raw-name count conflicted with building links from the parsed origin)
  - **≥32-char shared-secret check, value-asserted not identifier-counted.** A bare `grep -c 'MIN_SECRET_LEN'` was vacuous on `$B` (send-email already declares it at :58, so an unmodified copy passes) and on `$A` proved only that the token appears — `const MIN_SECRET_LEN = 8;`, or a declared-but-unused constant, passed. Static part, one statement each on **both** files (`$A/handler.ts` and `$B/index.ts`), every pattern **comment-filtered** via `grep -nE … | grep -vE '^[0-9]+:[[:space:]]*(//|\*)' | grep -q .`: `MIN_SECRET_LEN[[:space:]]*=[[:space:]]*32` and — **bound to the actual secret, not any `.length`** — `WORKLOG_EMAIL_SHARED_SECRET[[:space:]]*\.length[[:space:]]*<[[:space:]]*MIN_SECRET_LEN`. A bare `\.length < MIN_SECRET_LEN` could be satisfied by an unrelated or dead comparison.
    **Static is not sufficient — the enforcement is proven behaviourally**, in the same `handler.test.ts` harness: a **31-char** `WORKLOG_EMAIL_SHARED_SECRET` (canonical `WORKLOG_WEBSITE_URL`, everything else valid) → assert the request is rejected AND the quota RPC / `generateLink` / `fetch` recorded zero calls; a **32-char** secret → positive control, not rejected for that reason. The equivalent pair runs against `$B`'s exported handler for the send side. Syntax alone never proves a comparison gates anything. **Pre-fork counts: `$A` = 0 for both patterns** (register-company:255 is `if (!EMAIL_SHARED_SECRET)`, truthiness only — the register-side check is entirely new code), `$B` = 1 and 1, so the `$B` assertions are non-vacuous only as *regression* guards and are labelled as such.
  - **Enumeration parity on the signup-race path** (:382-402 — the branch a live-fetch schedule cannot deterministically trigger, so it is gated structurally rather than left to chance): **proven behaviourally, because occurrence counts are reachable-code-vacuous** — two *dead* `handleExistingAccount` calls satisfy a `-eq 2` count while an active race-specific return still leaks account state. In `handler.test.ts`, drive both duplicate paths through the injected `deps` and `diff` the results: (a) *ordinary duplicate* — the profile lookup returns a row; (b) *forced signup race* — the profile lookup returns nothing, then `generateLink` returns a 422 / "already registered" error and the re-lookup returns a row. **Assert both responses are byte-identical to the fresh-registration 200** (status, headers that vary with path, and body). Keep the comment-filtered counts (`CODEN()` = `grep -nE "$1" $A/handler.ts | grep -vE '^[0-9]+:[[:space:]]*(//|\*)' | wc -l`; `return await handleExistingAccount\(` → 2, forks of :359 and :388; `const okResponse = ` → 1) only as cheap secondary regression signals, explicitly not as the proof.
  - Branding, render-level (literal, non-vacuous by construction): `body=$(sed -n '/function shell(/,/^}/p' $B/index.ts); [ -n "$body" ]; printf '%s' "$body" | grep -q 'WorkLog'; ! printf '%s' "$body" | grep -qi 'C8102E'`
- [ ] Commit **and push** `worklog-register` (T3's branch build reads the pushed ref); do NOT deploy.

### Task 2b: App seam commit (WorkLog repo, worktree)

**Release ordering: this commit must not reach users before T4 deploys `worklog-register-company`** — shipped early, every registration hits a slug that does not exist yet.

- [ ] `src/auth/registration.ts`: `functions.invoke('register-company')` → `'worklog-register-company'` + `registration.test.ts` expectation; comment touch-ups naming the fork allowed. `register-company-*` **testIDs untouched** (app/(auth)/register.tsx, .maestro/register-validation.yaml — blanket rename breaks `maestroSelectors.test.ts`). `npm run verify` green. One commit.

### Task 3: Rehearse on a Supabase branch (USER APPROVAL: paid branch + deletion + may send real email)

**Preconditions:** `RESEND_API_KEY` usable from the branch (a send failure is not soft — send-email non-ok makes register-company throw at :432-434 and roll back the registration at :435-452). **A Supabase branch does not inherit production secrets**, so this key must be written to the branch explicitly: `POST /v1/projects/{branch-ref}/secrets` with `RESEND_API_KEY` — **a user-supplied Resend key collected at the T3 approval** — do NOT plan on recovering the production value from `GET /v1/projects/{ref}/secrets`, whose plaintext availability is not established and must not be depended on (and copying a production credential into a preview branch is a needless widening besides). If the user has no key to supply: descope T3 to deploy + 400/429-shape checks with mail assertions deferred to T5, recorded in the audit doc. **No secret value — this key, `WORKLOG_EMAIL_SHARED_SECRET`, or any other — is ever pasted into `docs/worklog-fork-report.md`, the audit doc, or any commit; evidence records names and outcomes only.**

**Sender descope (mirrors T5's).** If the T1 sender decision was `onboarding@resend.dev`, Resend delivers **only** to the Resend account owner's own address, so every `wl-a…wl-d` alias curl gets a non-ok from `worklog-send-email`, register-company throws at :433, rolls back, and returns 500 — i.e. *every* T3 assertion fails on a paid branch for a configuration reason. In that case T3's mail-bearing steps run against the **owner address** and are limited to **fresh(app) → admin-confirm → re-register (confirmed-dup)** — 2 email-quota units of 3. **The unconfirmed-dup/magiclink assertion is DEFERRED, not attempted:** once `email_confirm:true` is applied, that address can never again satisfy the `!email_confirmed_at` branch at register-company:296, and a second "fresh" registration on it is by definition a duplicate — so one owner address cannot produce both duplicate states. (This mirrors T5's descope; an earlier draft claimed all three run on one address and was impossible.) The 429 and bad-field checks use throwaway aliases, which need no delivery. Alternatively descope T3 entirely to deploy + 400/429-shape checks with all mail assertions deferred to T5. Decide at the T3 approval and record which.

**Branch keys:** branch anon and `service_role` keys come from `GET /v1/projects/{branch-ref}/api-keys?reveal=true` (MCP exposes publishable keys only). The confirmed-dup step's `email_confirm: true` is executed as a curl, not a JS SDK call: `PUT {branch-url}/auth/v1/admin/users/{id}` with `Authorization: Bearer <branch service_role>` + `apikey:` and body `{"email_confirm":true}`.

- [ ] Create branch (`create_branch` after `get_cost`/`confirm_cost`). Caveat: migration-built, NOT a clone of the drifted hosted schema — mechanics only.
- [ ] **Apply missing WorkLog migrations** — defined as: the set from `../jobsight-backend/supabase/migrations` that the branch build did not already apply, determined by diffing `list_migrations` on the branch against that directory on `worklog-register`; if the diff is empty, record "none" rather than skipping the check. (Branch builds run migrations from the **pushed** ref — see T1's git-integration bullet; T2 must have pushed `worklog-register`.) **Branch GoTrue `uri_allow_list`** (management API PATCH, branch ref, `$SUPABASE_ACCESS_TOKEN`) gets **`ADDED` — the justified entry set fixed at T1, minus whatever the branch already has, computed per the shared-config mutation discipline, which governs branch PATCHes as well as production** — BEFORE any round-trip. (An earlier draft said "the three WorkLog entries" here, contradicting both the minter rule and the missing-subset rule.)
- [ ] Deploy both functions via `POST /v1/projects/{branch-ref}/functions/deploy?slug={slug}` (multipart, metadata `{name, entrypoint_path, verify_jwt}` — the canonical shape above); GET each function record and assert `verify_jwt` true/false as intended. Branch secrets via `POST /v1/projects/{branch-ref}/secrets`: `WORKLOG_WEBSITE_URL`, fresh 44-char `WORKLOG_EMAIL_SHARED_SECRET`, `WORKLOG_RESEND_FROM` per T1 decision.
- [ ] Round-trips per the Global Constraints curl schedule (branch URL/keys; aliases user-controlled): fresh(app) → 200 `{ok:true}` + profile/company/**membership** rows + email whose CTA `action_link` `redirect_to` is host `worklog-site.vercel.app` path `/welcome`, sender `WORKLOG_RESEND_FROM`, WorkLog branding; **confirmed-dup**: the function returns only `{ok:true}`, so first **obtain the user id** — `execute_sql` on the branch: `select id from auth.users where email = '<alias>'` — then confirm it with the curl form named above (`PUT {branch-url}/auth/v1/admin/users/{id}`, branch `service_role` bearer + `apikey`, body `{"email_confirm":true}`; **not** a JS `updateUserById` call, which has no runner here), then re-register → byte-identical 200 (`diff` saved bodies) + `account_exists` mail; fresh(web) → same contract; **unconfirmed-dup** → magiclink mail landing on `/welcome` (link-shape only — production-configured site can't complete a branch token; end-to-end is T5's); bad-field → 400 `{error, field}`; ×4 same alias → 429.
- [ ] Record evidence in the audit doc (names and outcomes only, never secret values); delete the branch (covered by this approval).
- [ ] **T3 cleanup ladder — branch deletion is the cleanup, and its failure is not ignorable.** Any partial failure inside T3 (secrets partly written, one function deployed, branch allowlist PATCH failed, user abort) needs no per-artifact rollback *provided the branch is deleted*, since every artifact lives only on the branch — so on any T3 failure, go straight to deletion. **If `delete_branch` itself fails: STOP and escalate immediately** — a surviving preview branch is billable and holds a Resend key, a service-role key, and a shared secret; name the branch ref and every credential written to it so the user can revoke and retry. Never leave a failed T3 without either a confirmed deletion or an escalation naming the residue. Nothing in T3 touches production, so no production rollback is in scope here.

### Task 4: Production config + deploy (USER APPROVAL naming every change — all additive)

Approval lists exactly: four new `WORKLOG_*` secrets (or three if `WORKLOG_ALLOWED_ORIGINS` stays unset) — **disclosing that `POST /v1/projects/{ref}/secrets` restarts every edge function in the shared project (values untouched, but a shared-runtime blip the other tenant will experience)**; two function deploys (vacant slugs, `verify_jwt` per Global Constraints, via `/functions/deploy?slug=` multipart); `uri_allow_list` append of **`ADDED` — the justified entries not already present, named verbatim in the approval text**, together with the disclosure that `uri_allow_list` is project-global, so each appended entry also becomes a legal `redirect_to` for the other tenant's GoTrue links. Pre-conditions: T1's website check re-run green, AND **just-in-time vacancy re-checks** (re-run `list_edge_functions` + secrets name listing immediately before writing — abort on any collision that appeared since T1). In order:

- [ ] **Allowlist first, missing-subset only** (per the shared-config mutation discipline — an earlier draft appended all three unconditionally and rolled back all three, which could duplicate an entry or delete one the other tenant owns): re-read the live auth config NOW; compute `ADDED = justified entries (T1) − entries already present`; **record `ADDED` — it is the sole rollback ownership set**; if `ADDED` is empty, skip the PATCH and record that as success. Otherwise PATCH `uri_allow_list` = live value + `ADDED`; GET back and assert every member of `ADDED` present byte-for-byte, every pre-existing entry still present, and every other auth field byte-identical to the pre-PATCH snapshot (GoTrue matches `redirect_to` exactly — this is what keeps registrants off the shared Site URL).
- [ ] Secrets via `POST /v1/projects/{ref}/secrets`.
- [ ] Deploy `worklog-send-email` (`verify_jwt=false`) then `worklog-register-company` (`verify_jwt=true`); GET each and assert the flag.
- [ ] **Rollback is a triggered procedure, not a note.** Trigger: any failed write, any read-back assertion that disagrees, or user abort. Run in reverse dependency order, continuing past individual failures and reporting all of them: (1) `DELETE /v1/projects/{ref}/functions/{slug}` for whichever of the two deployed — deleting a never-deployed slug is a no-op, not an error; (2) `DELETE /v1/projects/{ref}/secrets` for exactly the `WORKLOG_*` names created (re-read the names list first — delete only what this run created; **this is the second shared-runtime restart, disclosed in the approval**); (3) allowlist: re-read the then-current value NOW and **first compare the full live auth config against this run's recorded post-PATCH state**. If anything differs — the allowlist or any other field — **STOP without PATCHing** and escalate naming the diff: another tenant has edited since our write, and per the concurrency limit above we can no longer prove `ADDED` is ours alone to remove. Only when the live state matches our post-PATCH record, remove **exactly the recorded `ADDED` set** — never "the three", never a pre-existing entry, and **never** PATCH back a stored snapshot. If `ADDED` is empty, or the failure occurred before the PATCH succeeded, there is nothing to remove and that is success, not an error. Abort + escalate if a member of `ADDED` is absent or appears twice. Read back after each step and assert it took.
- [ ] **Partial-state ladder (each has one defined outcome, no improvisation):** PATCH succeeded then secrets failed → roll back secrets + allowlist, no functions to remove. Some secrets created, others failed → the created ones are named in the delete list; roll back all three levels. First deploy succeeded, second failed → **delete the deployed one** (a live `worklog-send-email` with no `worklog-register-company` is inert but is an undeclared artifact in a shared project), then roll back secrets + allowlist. Post-deploy `verify_jwt` read-back disagrees → treat as deploy failure, delete both functions, full rollback — never "fix it with a redeploy" without re-obtaining approval. A rollback step that itself fails → STOP and escalate with the exact residual state named; do not proceed and do not retry silently.

### Task 5: End-to-end production validation (USER APPROVAL: sends real email)

**Sender branch — resolve BEFORE running any curl.** If T1 chose a `worklog@{verified domain}` sender, run the schedule below verbatim with the `kubiknyc+wlN@gmail.com` aliases. **If T1 chose `onboarding@resend.dev`, the aliases below are unusable** (delivery is restricted to the Resend account owner's own address): substitute the owner address and run **exactly these curls, in order: (0)** the JWT preflight bad-field curl → 400 not 401 (mandatory in every variant, full or descoped; 0 quota units); **(1)** fresh(app) `{client:'app'}` → 200, mail asserted; **(2)** admin-confirm the user (not a function call, **0 quota units**); **(3)** re-register the same address → byte-identical 200 + `account_exists` mail; **(4)** bad-field → 400 (rejected at :204-215, **0 quota units**). **Total 2 email-quota units of 3 on that address** (an earlier draft said three, miscounting the admin-confirm as a curl), 2 IP units. The magiclink step and the `client:'web'` step **cannot** run on a single address — once `email_confirm:true` is applied the `:296` branch is closed forever, and a second "fresh" registration is by definition a duplicate — so both are explicitly DEFERRED. **`onboarding@resend.dev` is therefore NOT a completion path for this plan's goal** — it ends T5 in a DEFERRED state, and registration is not declared LIVE. Its closing phase, stated at the T1 gate so the choice is made with its cost visible: user verifies a Resend domain → update `WORKLOG_RESEND_FROM`, which is a write to an **existing** secret name and therefore a replace-class change needing its own approval → re-run the full alias schedule plus the app-UI pass → only then LIVE. Record the chosen branch in the audit doc.

**Alias headroom:** `+wl1` and `+wl3` each consume 2 of their 3 daily email-quota units, and production quota rows are never reset — so exactly one retry is available per address. If a step needs a second retry, use a **fresh unused alias** (`+wl4`, `+wl5`, …) rather than waiting out the 24h window or resetting anything.

- [ ] `kubiknyc+wl1@gmail.com`, `client:'app'`: email arrives — WorkLog branding, sender per T1, no PunchLog strings, no dead links; CTA lands on `/welcome`; password set succeeds; app sign-in works.
- [ ] `kubiknyc+wl2@gmail.com`, `client:'web'`: same assertions.
- [ ] Confirmed-dup: re-register `+wl1` → byte-identical 200 + WorkLog `account_exists` mail with no forgot-password link.
- [ ] **Magiclink recovery** (closes T3's deferral): `+wl3` fresh, do NOT confirm, re-register → magiclink mail → `/welcome` (neutral 'other' copy per welcomeLink.ts:59-64) → password set succeeds → sign-in works. **Save both `+wl3` bodies and `diff` them** — the unconfirmed-duplicate response must be byte-identical to the fresh one, the same enumeration guarantee already asserted on the confirmed-dup path.
- [ ] Bad-field → 400 shape. **7 curls total: the 6 scheduled plus the JWT preflight bad-field curl.** Both bad-field curls are rejected at :204-215 **before** the quota RPC at :232, so they consume **zero** quota of either kind — the schedule is **5 quota-consuming curls: 5 IP units of 20**, and 5 email units spread as `+wl1` 2, `+wl2` 1, `+wl3` 2, each within the 3/address limit. (An earlier draft counted the preflight as 1 IP unit, contradicting the pre-quota ordering it cites in the same sentence.)
- [ ] **Drive the app's own register screen once** (not just curls): the curl schedule proves the function contract, but only the app UI proves the shipped `functions.invoke` + shipped anon key work against `verify_jwt=true`. Reuse the `+wl1` account's sign-in, or run one additional fresh alias through the UI. Registration is not declared LIVE on curl evidence alone.
- [ ] Update memory + spec status; registration LIVE.

## Self-review notes

- Loop-2 round-2 findings all addressed: gate is fail-closed (`set -euo pipefail`, test-assertions, occurrence-based patterns with recorded pre-fork counts proving non-vacuity, extraction-not-filtering for secret names, render-level branding slice); deploy endpoint corrected to `/functions/deploy` multipart with post-deploy flag assertion; secrets endpoint + `$SUPABASE_ACCESS_TOKEN` named once in Tech Stack and collected at the T1 gate; `ALLOWED_ORIGINS` namespaced (`WORKLOG_ALLOWED_ORIGINS`) with the shared-read carve-out list now exhaustive (`RESEND_API_KEY` only); URL validation extended to port/search/hash with links built from the parsed origin; onboarding@resend.dev restriction surfaced at the gate + T5 descope; `handle_new_user`-no-email escalation branch added; explicit curl schedules; T2 report artifact defined; forbidden-link removal enumerated at its four real source lines (129/189/197/225).
- Loop-3 round-4 findings (both reviewers FAIL) addressed: **a fourth false-green in the A1 gate, found and closed.** `handler.test.ts` asserted only "hostile `WORKLOG_WEBSITE_URL` ⇒ 503 + zero probe calls" — but the fork keeps the shared-secret guard (fork of :255-257), which returns 503 with zero calls too, so a fork with **no validator at all** passed on the wrong 503. The test now supplies a full valid env fixture (≥32-char `WORKLOG_EMAIL_SHARED_SECRET`, `SUPABASE_URL`, service key), adds a **positive control** (canonical URL ⇒ not 503, probes DO fire) so an unconditionally-503ing handler fails, and asserts a marker distinguishing the origin 503 from the secret 503. **The prescribed structure also could not run** — it kept `Deno.serve` in the file the test imports, contradicting the plan's own reason for splitting `origin.ts`; logic moved to `$A/handler.ts` (with an explicit `createHandler(deps)` injection seam for the admin client, `fetch`, and env, since the source builds its client inline at :218 and "assert zero calls" had nothing to observe), leaving `index.ts` as `Deno.serve(handler)` only. Every structural grep is now comment-filtered with the **anchored** `^[0-9]+:` prefix (unanchored, it discarded code containing `https://`), `S` anchors on `await sendEmail(` so a `function sendEmail(` rewrite cannot re-anchor on a definition, and a non-zero gate exit is a **hard blocker on entering T3**. T3's branch allowlist now uses `ADDED` (it said "the three entries", contradicting both the minter and missing-subset rules). Added: a T1 step that owns the `worklog://confirm` minter search; a T1 check that the shipped anon key is a JWT (a `sb_publishable_…` key would 401 the app's own `functions.invoke` against `verify_jwt=true` while curls with a legacy JWT pass); a T5 app-UI pass, since curls never exercise the shipped client; the honest statement that the allowlist read-modify-write cannot be made atomic, so concurrency is a STOP not a retry and rollback escalates rather than removing when the live value moved; T5 recounted to 7 curls including the JWT preflight; `onboarding@resend.dev` declared explicitly not a completion path, with its closing phase named.
- Loop-3 round-3 findings addressed: **A1's gate is now a handler-level behavioural proof.** Three successive gate designs were false-green for the same reason — they tested *presence* rather than *invocation before side effects*: `grep 503` (the unforked source 503s at :255-257, and after the quota RPCs), a hostname line-anchor + word window (satisfiable by the header comment T2 orders rewritten), and an exported-validator unit test whose ordering anchor matched the module-scope **definition**, passing on a fork that never calls it. The fix splits the validator into `origin.ts`, requires `index.ts` to export its handler (`Deno.serve` at :178 otherwise boots a listener on import), and adds `handler.test.ts`: hostile `WORKLOG_WEBSITE_URL` → assert 503 **and zero calls** to the quota RPC, `generateLink`, and `fetch`. Also fixed: `--allow-none` is not a Deno flag; `:443` cannot be rejected via a parsed port (`URL` normalizes it to `""`) so the validator checks the raw authority; the `CODE()` comment filter is anchored to the line-number prefix (unanchored, it discarded every code line containing `https://`). **T4's allowlist steps now follow the missing-subset rule** they contradicted — `ADDED` is computed pre-PATCH and is the sole rollback ownership set, so no pre-existing entry can be duplicated or deleted. Deploy metadata reconciled to `name` (with `slug` as the query param) everywhere. `MIN_SECRET_LEN` is value-asserted and comparison-asserted rather than identifier-counted, with `$A` pre-fork counts of 0 recorded. A6's dangling "or name a web deployment" removed. **Allowlist entries now require a live minter:** `/welcome` (A1) and `worklog://set-password` (`AuthProvider.tsx:306`) qualify; `worklog://confirm` has none in the app repo and is dropped unless T1 finds one — and the cross-tenant redirect-surface widening is disclosed in T4's approval. Plus: signup-race enumeration parity gated structurally, T5's owner-address variant recounted (2 units, not 3) with its curls enumerated, anon-key-must-be-a-JWT precondition added, `GET /secrets` corrected to digests-not-values, CORS anchor corrected to :144-151.
- Loop-3 round-2 findings addressed: **A1's gate is now behavioural** — a named exported `assertCanonicalWorklogOrigin` exercised by `deno test` against hostile inputs, because both grep drafts were false-green-reachable (the second one satisfiable by the very header comment T2 orders rewritten); static ordering survives only as secondary evidence, with `Q`/`G`/`S` actually assigned (the round-1 draft compared unbound variables and would have aborted under `set -u`), `head -1` not `tail -1` (a validator between the IP RPC at :232 and the email RPC at :243 previously passed), and comment-stripped extraction. Round-1's T3 owner-address descope was **impossible** — one address cannot be both confirmed- and unconfirmed-duplicate once `email_confirm:true` closes the `:296` branch — now limited to fresh → confirm → confirmed-dup with the magiclink assertion deferred, mirroring T5. Corrected a false premise round 1 introduced: `npm run verify` does **not** read the sibling clone (`package.json:15`); the migration branch now specifies merge-first ordering plus the `LOCAL_ONLY`/`SERVER_ONLY` obligation. Also: user-id acquisition for the admin-confirm and the curl-vs-SDK contradiction resolved; branch `RESEND_API_KEY` now user-supplied rather than assuming production plaintext is readable; no secret value may enter any artifact; allowlist pre-existence became a missing-subset append with run-scoped rollback instead of a dead-end STOP; A6 narrowed to defer-or-STOP; T3 gained its own cleanup ladder with branch-deletion-failure escalation; the ≥32-char check is now gated on both sides (register-company:255 is truthiness-only today).
- Loop-3 round-1 findings addressed: **retired the false-green `grep -qE "503"` gate check** — the unforked source already 503s at register-company:257, so A1's canonical-origin validator could be omitted entirely and the gate still passed; replaced with a slice-content + line-number dominance assertion (`V < Q`, `V < G`, `V < S`) that proves what A1 actually claims. Added `mailer_autoconfirm` to T1's GoTrue audit with a STOP (autoconfirm ⇒ anonymous confirmed-account creation, and the magiclink branch at :296 unreachable). Allowlist append now requires the three entries to be **absent** first, a fresh pre-PATCH read, and a post-PATCH byte-identical assertion on every other auth field — "Site URL untouched" was intent-only; the GoTrue reload is now disclosed. T4 gained a triggered rollback procedure and a partial-state ladder. T3 gained the sender descope it lacked (only T5 had one), explicit branch `RESEND_API_KEY` provisioning, `api-keys?reveal=true` for the service-role key, a defined "missing migrations" set, and the pushed-ref/git-integration precondition. A2 "no" now terminates the plan explicitly. Management API shapes are spelled out once. Gate additions: exactly-one helper target, four call sites, pre-fork count 2 for `functions/v1/send-email` (including send-email:29, now in T2's comment-rewrite list), local `ALLOWED_ORIGINS` const rename.
- Out of scope: hosted drift reconciliation; universal links; invite flow.
